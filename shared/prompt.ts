export const PROMPT = {
  testcaseGeneration:
    "Generate manual UI test cases based on this screenshot. Return them in structured JSON format with keys: description, pre_condition, test_steps, expected_result",
};
