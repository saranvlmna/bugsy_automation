export const PROMPT = {
  testcaseGeneration:
    "Generate manual UI test cases based on this screenshot. Return them in structured JSON format with keys: title, objective, preconditions, steps, expectedResult",
};
